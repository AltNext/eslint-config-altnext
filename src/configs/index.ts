import base from './base';
import playwright from './playwright';
import typeChecking from './type-checking';

export { base, playwright, typeChecking };

export const configs = { base, playwright, typeChecking };
